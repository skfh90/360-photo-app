pluginManagement {
    repositories {
        maven { url uri("${rootDir}/offline/maven-repo") }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'PhotoSphere'
include ':app'
